#include <stdio.h>
#include "main.h"

/**
 * puts_half - prints second half of a string followed by a new line
 *
 */

void puts_half(char *str)
{
	int i;
	int len = 0;

	for (i=0; str[i]!='\0'; i++)
	{
		len++;
	}
	/*n = (len - 1) / 2;*/
}
	
