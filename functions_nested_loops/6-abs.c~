#include "main.h"

/**
 * _abs - Entry point,computes the absolute variable of n
 * @a: character to compute
 *
 * Return: 0
 */

int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}

	else
	{
		return (-a);
	}
	
}
