#include <stdio.h>
/**
 * main - prints all single digits in base 16
 * followed by a new line
 * Return: 0
 */
int main(void)
{
	char base_16 = 0;

	while (base_16 <= 15)
	{
		putchar(base_16);
		base_16++;
	}
	putchar('\n');
	return (0);
}
